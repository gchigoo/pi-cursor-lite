---
doc_type: goal-iteration
goal: publish-github-npm
iteration: 7
status_after: complete
next_action: none; goal complete
blocker_signature: null
functional_acceptance: ../functional-acceptance.md
updated_at: "2026-07-30"
---

# Iteration 007

## Current Understanding

owner 已批准完整 0.1.1 release sequence；实现、独立 code review 和本地 prepublish gate 均已通过。本 iteration 负责外部交付、真实 registry/Pi 验证和独立终端功能验收。

## Implementation Approach

严格按获批顺序执行：scoped commit/push → GitHub provenance 核验 → main npm publish → 0.1.0 deprecate → 本机 Pi 切换 → registry/consumer/Pi 验收 → 独立 Grok 终端验收。

## Changes This Iteration

- 提交 `1600d67`（`fix: secure shell runtime and npm dependency tree`），包含 0.1.1、shell/context 修复、security fork source、tests 和流程证据。
- 推送 GitHub main，并确认 origin/local commit 一致、public fork manifest 可见。
- Public npm publish `pi-cursor-lite@0.1.1`；latest 已指向 0.1.1。
- Deprecated `pi-cursor-lite@0.1.0`，消息明确其使用 vulnerable `undici@5.29.0`，并要求先 remove 旧包再安装 0.1.1。
- 本机 Pi 先尝试直接升级；检测到共享 npm root 保留 0.1.0 lock/hoist 后立即停止并诊断。
- 按批准的卸载/重装路径移除旧包，确认根 audit=0，再安装 `npm:pi-cursor-lite@0.1.1`。
- 启动独立 Grok 4.5 High terminal acceptance，结果为 PASS。

## Verification Evidence

- Publish 期间自动 `prepublishOnly`：64 tests、typecheck、boundaries、manifest、source/packed load、fork hash/import/audit、packed consumer alias/dedupe/audit 全部通过。
- Registry latest：`pi-cursor-lite@0.1.1`；dist integrity `sha512-nCJ9cQGrLgzVUzhld2h3pr2todvAhX+6Dps0qhPFBxoJL2THg76vxipPendi04w65DBC6oIsOOFuJMNDwh0+4Q==`。
- Registry consumer：Cursor SDK deduped 到 `@gchigoo/connect-node@1.7.1`；只含 `undici@6.27.0`；audit=0；Pi load 通过。
- 本机 `pi list`：`npm:pi-cursor-lite@0.1.1`，无本项目 local source。
- 本机 Pi npm root：单一 fork alias node、Undici 6.27.0、audit=0。
- `pi --list-models cursor-lite`：`cursor-lite / auto / 200K / 16.4K`。
- 独立终端验收：`.codestable/goals/2026-07-28-publish-github-npm/functional-acceptance.md`，verdict=pass。

## Problems Encountered

直接执行 `pi install ...@0.1.1` 会在 Pi 共享 npm root 中保留 0.1.0 的 hoisted official Connect Node，安装命令报告 1 high + 3 moderate。实际树证明 0.1.1 fork 位于主包子树，而 Cursor SDK 仍引用旧根节点。执行批准范围内的 `pi remove npm:pi-cursor-lite` 后，旧依赖被完整移除且 audit=0；重新安装 0.1.1 后 alias/dedupe 正确且 audit=0。

该现象是 npm 共享根增量升级残留，不影响干净 0.1.1 消费者，但说明从 vulnerable 0.1.0 升级必须卸载后重装。

## Next Attempt

none；所有 owner acceptance criteria 已满足。后续 SDK 升级时重新核验 Connect Node alias contract、consumer audit 和 shell executor 初始化时序。

## State Update

Goal 标记 complete；本报告与 `functional-acceptance.md` 双向引用，`current_iteration=7`。
