---
doc_type: goal-iteration
goal: publish-github-npm
iteration: 2
status_after: active
next_action: 完整独立复审后创建首次提交并发布 GitHub
blocker_signature: null
functional_acceptance: null
updated_at: "2026-07-28"
---

# Iteration 002

## Current Understanding

首次 reviewer 指出的凭证文档和 packed load 缺口均属真实发布风险；必须在外部 push/publish 前关闭。

## Implementation Approach

统一中英文凭证路径与 Pi 内 `/login` 语义；让通用 Pi loader script 接受任意 extension path，并由 packed test 对临时安装后的入口执行真实注册验证。

## Changes This Iteration

- English/中文 README 统一使用 `~/.pi/agent/auth.json` 和 Pi 内 `/login`。
- `scripts/test-pi-load.mjs` 支持传入已安装 extension path。
- `scripts/test-packed.mjs` 对临时安装后的 `src/index.ts` 执行 Pi loader，并断言 `cursor-lite`/`auto` 注册。
- `prepublishOnly` 纳入 `npm run test:packed`。

## Verification Evidence

- `npm run test:pi-load`：源码入口加载通过。
- `npm run test:packed`：13 files、15,628 bytes；临时安装、双语文档、LICENSE 和 packed Pi load 全部通过。
- package-lock 已同步 public manifest 元数据。

## Problems Encountered

none

## Next Attempt

对修订后的完整发布范围执行独立复审；通过后建立首次提交并推送 public GitHub。

## State Update

Goal 保持 active；current iteration 更新为 2。
