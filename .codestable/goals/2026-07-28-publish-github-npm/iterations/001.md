---
doc_type: goal-iteration
goal: publish-github-npm
iteration: 1
status_after: active
next_action: 执行独立发布审查并修正 blocking finding
blocker_signature: null
functional_acceptance: null
updated_at: "2026-07-28"
---

# Iteration 001

## Current Understanding

发布前必须同时解决首次提交安全、npm 可发布元数据、双语文档和 packed artifact 完整性；不能把本地 Pi 凭证或依赖目录带入公开仓库。

## Implementation Approach

先建立 `.gitignore` 与发布元数据，再完成 English/中文 README 和 LICENSE；所有外部写操作留到本地验证与独立审查通过之后。

## Changes This Iteration

- 新增根 `.gitignore`，排除 `.pi/`、`node_modules/`、tarball、日志、env 和 Windows `nul`。
- 新增 MIT `LICENSE`，公开作者名统一为 Gchigoo。
- `package.json` 改为 public package，补 author/repository/homepage/bugs/license/publishConfig 和 `prepublishOnly`。
- 新增 `README.zh-CN.md`，并更新 English README 的 npm 安装命令和语言入口。
- 更新 manifest/packed verifier，覆盖公开元数据、双语 README 与 LICENSE。

## Verification Evidence

- `npm run check`：8 个测试文件、54 个测试通过。
- boundaries、manifest、Pi loader、packed install：通过。
- `npm audit --omit=dev`：0 vulnerabilities。
- `npm pack --dry-run`：13 files、15,619 bytes；仅包含 LICENSE、双语 README、package.json 和 9 个 `src` 文件。
- `npm publish --dry-run --ignore-scripts --json`：`pi-cursor-lite@0.1.0`，public/latest，成功。
- commit candidate credential-shape scan：无匹配；本地目录和凭证资产均被 ignore。

## Problems Encountered

- manifest verifier 原先强制 `private: true`，已改为验证 public package 所需字段。
- `npm publish --dry-run --json` 的 lifecycle stdout 与 JSON 混合；先单独完成 prepublish 验证，再用 `--ignore-scripts` 验证发布 payload。

## Next Attempt

启动隔离只读 reviewer 审查首次公开发布范围，修正所有 material finding 后再 commit/push。

## State Update

Goal 保持 active；current iteration 更新为 1。
