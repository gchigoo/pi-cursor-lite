---
doc_type: goal-iteration
goal: publish-github-npm
iteration: 6
status_after: blocked
next_action: 等待 owner 授权 0.1.1 release sequence
blocker_signature: release-0.1.1-external-actions
functional_acceptance: null
updated_at: "2026-07-29"
---

# Iteration 006

## Current Understanding

owner 已批准并完成 scoped Connect Node fork publish；下一终态是把主包 0.1.1 的源码、registry、deprecated 旧版本和本机 Pi 安装同步到安全状态。外部 release 动作尚未获得本轮明确授权。

## Implementation Approach

- 主包升级为 0.1.1，通过 npm alias 直接依赖 `@gchigoo/connect-node@1.7.1`。
- 移除不传播的 root override。
- packed gate 必须从干净消费根断言 alias target、Cursor SDK dedupe、单一 `undici@6.27.0`、audit=0 和 Pi load。
- 独立 Grok 4.5 High 审查完整 worktree；review 后仅做 test/hash evidence focused closure。

## Changes This Iteration

- Public npm publish `@gchigoo/connect-node@1.7.1`；registry 已可见。
- `package.json` / lock 切换到 0.1.1 与安全 alias。
- 双语 README 更新安装版本和依赖安全说明。
- `test-packed.mjs` 增加真实 alias/dedupe/Undici/audit 断言，并修复 `--omit=peer` 误删 protobuf 的测试安装参数。
- `test-connect-node-fork.mjs` 改为使用官方 1.7.0 tarball生成的 53 项冻结 SHA-256 清单，不再依赖当前 alias node_modules 自比。
- 写入 `publish-github-npm-review.md`，记录 Grok findings 与 focused closure。

## Verification Evidence

- Registry：`@gchigoo/connect-node@1.7.1` public、依赖 `undici@6.27.0`。
- Fork dry-run shasum/integrity 与发布输出一致。
- `npm ls`：Cursor SDK deduped 到 alias；根开发树 production audit=0。
- `npm run test:packed`：一个 Connect Node alias 节点、仅 Undici 6.27.0、consumer audit=0、Pi packed load 通过。
- `npm run prepublishOnly`：9 files / 64 tests、typecheck、boundaries、manifest、source load、packed load、fork verification、repository/consumer audits 全部通过。
- Grok 初审发现两个 High/Medium gate 缺口；对应 delta 仅为 tests/hash evidence，focused closure 后 code review=passed。

## Problems Encountered

- npm publish 成功后的首分钟 public metadata 短暂 404；确认 package status public 与 dist-tag 后等待传播，随后 public registry 正常。
- `--omit=peer` 会把被 peer 标记但也由 SDK 直接依赖的 protobuf 省略；改用 `--legacy-peer-deps` 仅阻止宿主 Pi peer 自动安装，运行依赖完整。
- Grok 指出 fork 未 commit/push 时 provenance URL 尚不可审计；因此必须先 push 源码，再发布主包。

## Next Attempt

等待 owner 批准 0.1.1 release sequence：scoped commit/push → main npm publish → deprecate 0.1.0 → 本机 Pi 切换及终端功能验收。

## State Update

Goal 因不可逆外部 release 动作进入 `blocked / RiskAcceptanceNeeded`；本地实现和审查均已完成，可安全恢复。
